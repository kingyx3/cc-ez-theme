{
  "name": "cc-attribution-worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "d1:migrate:local": "wrangler d1 migrations apply cc-attribution --local",
    "d1:migrate:remote": "wrangler d1 migrations apply cc-attribution --remote"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
