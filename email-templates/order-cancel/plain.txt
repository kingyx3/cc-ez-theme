{% if customer.name == blank %}
Hi there,
{% else %}
Hi {{ customer.name }},
{% endif %}

{% if order.cancel_reason == blank %}
  {% if order.is_paid == false %}
It looks like payment for order {{ order.name }} wasn't completed, so the order was automatically cancelled and the stock released.

If you'd still like the items, no worries. You can simply place a new order at https://cardboard.sg and complete payment at checkout.

Already reordered? You can ignore this email.
  {% else %}
We're writing to let you know that your order {{ order.name }} has been cancelled.

You can visit our store at https://cardboard.sg.
  {% endif %}
{% else %}
We're writing to let you know that your order {{ order.name }} has been cancelled.

Cancellation reason: {{ order.cancel_reason }}

You can visit our store at https://cardboard.sg.
{% endif %}

Warm regards,
Cardboard Collective
